<h1>Books</h1>

You have  <%= Article.public_count %> Book!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "Add Book", new_article_path %>
